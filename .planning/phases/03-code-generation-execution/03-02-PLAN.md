---
phase: 03-code-generation-execution
plan: '02'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - src/processing/code_executor.py
  - src/processing/__init__.py
  - src/routes/+page.svelte
autonomous: true
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-04
  - EXEC-05

must_haves:
  truths:
    - "User can run generated code with custom input"
    - "Execution results displayed clearly"
    - "Execution errors shown in user-friendly format"
    - "Timeout protection prevents infinite loops"
    - "Memory limits prevent runaway processes"
  artifacts:
    - path: "src/processing/code_executor.py"
      provides: "Safe code execution with limits"
      exports: ["execute_python", "ExecutionResult"]
  key_links:
    - from: "src/processing/code_generator.py"
      to: "src/processing/code_executor.py"
      via: "CodeGenerationResult.code input"
      pattern: "def execute_python.*code.*str"
    - from: "src/routes/+page.svelte"
      to: "src/processing/code_executor.py"
      via: "Tauri invoke call"
      pattern: "invoke.*execute_python"
---

<objective>
Build the execution sandbox that runs generated Python code safely with timeout and memory protections.

Purpose: Execute user code in isolated subprocess with configurable limits per STACK.md (subprocess with limits approach).
Output: Execution sandbox module with result display
</objective>

<execution_context>
@/home/milgraph/Projects/algo/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md

# From Plan 01 output:
- CodeGenerationResult contains generated Python code
- Variable mapping for input adaptation

# Key tech from STACK.md:
- subprocess with limits (timeout, memory)
- No Docker needed (local-only, mathematician's own code)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define execution result types</name>
  <files>src/processing/code_executor.py</files>
  <read_first>
    - src/processing/__init__.py (to see existing exports)
  </read_first>
  <action>
    Create code_executor.py with:
    
    1. Import subprocess, signal, resource, io
    2. Define ExecutionResult Pydantic model:
       - stdout: str
       - stderr: str
       - return_code: int
       - timed_out: bool
       - memory_exceeded: bool
       - execution_time_ms: int
       - error_message: Optional[str] (user-friendly)
    3. Define constants:
       - DEFAULT_TIMEOUT_SECONDS = 30
       - DEFAULT_MEMORY_MB = 512
    4. Export ExecutionResult and constants
  </action>
  <verify>
    <automated>python -c "from src.processing.code_executor import ExecutionResult; print('Import OK')"</automated>
  </verify>
  <done>ExecutionResult class defined and importable</done>
</task>

<task type="auto">
  <name>Task 2: Implement subprocess execution with limits</name>
  <files>src/processing/code_executor.py</files>
  <read_first>
    - src/processing/code_executor.py (already has types from Task 1)
    - src/processing/code_generator.py (to see code format)
  </read_first>
  <action>
    Add the execute_python function:
    
    1. Create execute_python(
       code: str,
       user_input: dict = None,
       timeout_seconds: int = DEFAULT_TIMEOUT_SECONDS,
       memory_limit_mb: int = DEFAULT_MEMORY_MB
    ) → ExecutionResult
    2. Setup:
       - Create temp file for code
       - Prepare stdin with user_input as JSON if provided
    3. Execute:
       - Run with subprocess.Popen
       - Set memory limit using resource.setrlimit(resource.RLIMIT_AS, ...)
       - Wait with timeout using subprocess.communicate(timeout=...)
    4. Handle errors:
       - TimeoutExpired → timed_out=True, user-friendly "Execution timed out after X seconds"
       - MemoryError → memory_exceeded=True, user-friendly "Memory limit exceeded"
       - Other exceptions → error_message with explanation
    5. Return ExecutionResult with all fields populated
    6. Cleanup: remove temp file
  </action>
  <verify>
    <automated>python -c "
from src.processing.code_executor import execute_python

# Test simple execution
result = execute_python('print(2 + 2)')
print('Output:', result.stdout.strip())
print('Return code:', result.return_code)
print('Timed out:', result.timed_out)
"</automated>
  </verify>
  <done>execute_python runs code and returns ExecutionResult</done>
</task>

<task type="auto">
  <name>Task 3: Wire execution to frontend</name>
  <files>src/processing/__init__.py, src/routes/+page.svelte</files>
  <read_first>
    - src/routes/+page.svelte (existing UI structure)
    - src/processing/__init__.py
    - math-algorithm-tool/src-tauri/src/main.rs (Tauri commands)
  </read_first>
  <action>
    1. Update src/processing/__init__.py to export:
       - from .code_executor import execute_python, ExecutionResult
    
    2. Add Tauri command in src-tauri/src/main.rs:
       ```rust
       #[tauri::command]
       fn execute_python(code: String, user_input: Option<String>) -> Result<ExecutionResult, String>
       ```
       - Parse user_input as JSON if provided
       - Call Python execute_python
       - Return JSON serialization of ExecutionResult
    
    3. Update +page.svelte to add "Run" button:
       - Add button in Code panel header
       - On click: call invoke('execute_python', {code: generatedCode, userInput})
       - Display results in Results section
       - Show timeout/memory error messages prominently
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend can invoke code execution and display results</done>
</task>

</tasks>

<verification>
- [ ] code_executor.py exists with ExecutionResult class
- [ ] execute_python function runs code in subprocess
- [ ] Timeout triggers after configured seconds
- [ ] Memory limit enforced
- [ ] Errors displayed in user-friendly format
- [ ] Frontend Run button triggers execution
- [ ] Results displayed in UI
</verification>

<success_criteria>
User can run generated code with custom input and see:
- Clear output display
- User-friendly error messages
- Timeout protection active
- Memory limit protection active
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-generation-execution/03-02-SUMMARY.md`
</output>
