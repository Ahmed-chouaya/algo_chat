---
phase: 03-code-generation-execution
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/processing/code_generator.py
  - src/processing/__init__.py
  - src/lib/types.ts
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - CODE-03
  - CODE-04

must_haves:
  truths:
    - "System generates syntactically correct Python code from algorithm steps"
    - "Generated code is executable without syntax errors"
    - "Variables follow mathematical notation conventions (x₁ → x_1)"
    - "Generated code includes comments explaining each section"
  artifacts:
    - path: "src/processing/code_generator.py"
      provides: "Code generation from AlgorithmStep objects"
      exports: ["generate_python_code", "CodeGenerationResult"]
    - path: "src/lib/types.ts"
      provides: "TypeScript types for code generation"
      exports: ["CodeGenerationResult"]
  key_links:
    - from: "src/processing/step_extractor.py"
      to: "src/processing/code_generator.py"
      via: "AlgorithmStep[] input"
      pattern: "def generate_python_code.*AlgorithmStep"
---

<objective>
Build the code generation engine that transforms algorithm steps from Phase 2 into executable Python code.

Purpose: Convert structured AlgorithmStep objects into syntactically correct, readable Python using AST-based code generation (ast + astor per STACK.md).
Output: Python code generator module with types
</objective>

<execution_context>
@/home/milgraph/Projects/algo/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md

# Phase 2 output - algorithm step structure
From src/processing/step_extractor.py:
```python
class AlgorithmStep(BaseModel):
    step_number: int
    description: str
    code_equivalent: str  # Pseudocode
    variables: list[Variable]
    control_flow: Optional[str]
    confidence: str
```

# Key tech from STACK.md:
- Code Generation: Python ast + astor 0.8.x
- astor.to_source() for readable AST-to-Python conversion
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define code generation types and contracts</name>
  <files>src/processing/code_generator.py</files>
  <read_first>
    - src/processing/step_extractor.py (to understand AlgorithmStep structure)
    - src/processing/__init__.py (to see existing exports)
  </read_first>
  <action>
    Create code_generator.py with:
    
    1. Import ast, astor, and AlgorithmStep from step_extractor
    2. Define CodeGenerationResult Pydantic model:
       - code: str (generated Python source)
       - syntax_valid: bool
       - variable_mapping: dict (original → Python-safe names)
       - errors: list[str] (any validation errors)
    3. Define variable name converter function:
       - Converts mathematical subscripts: x₁ → x_1, y₂ → y_2
       - Converts Greek letters: α → alpha, β → beta
       - Sanitizes invalid Python identifiers
    4. Export CodeGenerationResult
  </action>
  <verify>
    <automated>python -c "from src.processing.code_generator import CodeGenerationResult; print('Import OK')"</automated>
  </verify>
  <done>CodeGenerationResult class defined and importable</done>
</task>

<task type="auto">
  <name>Task 2: Implement AST-based code generation</name>
  <files>src/processing/code_generator.py</files>
  <read_first>
    - src/processing/code_generator.py (already has types from Task 1)
  </read_first>
  <action>
    Add the main generate_python_code function:
    
    1. Create generate_python_code(steps: list[AlgorithmStep]) → CodeGenerationResult
    2. For each step:
       - Create AST node with ast.Module + ast.FunctionDef
       - Add docstring from step.description
       - Add variable declarations from step.variables
       - Add step.code_equivalent translated to AST
       - Handle control_flow (for/while loops, if/elif)
    3. Use astor.to_source() to convert AST to readable Python
    4. Add section comments: "# Step N: {description}"
    5. Validate syntax using ast.parse() before returning
    6. Return CodeGenerationResult with code, syntax_valid=True, variable_mapping
  </action>
  <verify>
    <automated>python -c "
from src.processing.step_extractor import AlgorithmStep, Variable
from src.processing.code_generator import generate_python_code

# Test with simple step
step = AlgorithmStep(
    step_number=1,
    description='Initialize counter',
    code_equivalent='x = 0',
    variables=[Variable(name='x', type='int', initial_value='0')],
    control_flow=None,
    confidence='high'
)
result = generate_python_code([step])
print('Generated:', result.code)
print('Valid:', result.syntax_valid)
"</automated>
  </verify>
  <done>generate_python_code produces valid Python from AlgorithmStep list</done>
</task>

<task type="auto">
  <name>Task 3: Add TypeScript types for frontend</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts (check existing types)
  </read_first>
  <action>
    Add TypeScript equivalents for code generation:
    
    ```typescript
    export interface CodeGenerationResult {
      code: string;
      syntax_valid: boolean;
      variable_mapping: Record<string, string>;
      errors: string[];
    }
    ```
  </action>
  <verify>
    <automated>cd /home/milgraph/Projects/algo/math-algorithm-tool && npx tsc --noEmit src/lib/types.ts 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript types for CodeGenerationResult defined</done>
</task>

</tasks>

<verification>
- [ ] code_generator.py exists with CodeGenerationResult class
- [ ] generate_python_code function accepts AlgorithmStep list
- [ ] Output is valid Python syntax (ast.parse succeeds)
- [ ] Mathematical variable names converted (x₁ → x_1)
- [ ] Comments added for each step section
- [ ] TypeScript types exported
</verification>

<success_criteria>
Valid Python code generated from algorithm steps with:
- Syntax validation passed
- Mathematical notation converted to Python-safe names
- Comments explaining each section present
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-generation-execution/03-01-SUMMARY.md`
</output>
