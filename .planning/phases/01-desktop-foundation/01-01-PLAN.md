---
phase: 01-desktop-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - math-algorithm-tool/
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05
  - SET-06
must_haves:
  truths:
    - User can launch the desktop application and see a window
    - User can see an input panel for pasting algorithm descriptions
    - User can see an output panel for displaying results
    - Application runs without crashes on primary target platform
    - User can configure API keys for AI providers (NVIDIA, OpenAI, Anthropic)
    - User can select which AI provider to use
    - API keys are stored securely
  artifacts:
    - path: "math-algorithm-tool/"
      provides: "Tauri 2.0 + Svelte 5 desktop application"
    - path: "math-algorithm-tool/src/lib/components/"
      provides: "UI components (Header, SplitView, InputPanel, OutputPanel, SettingsModal)"
    - path: "math-algorithm-tool/src-tauri/src/main.rs"
      provides: "Rust backend with keyring integration"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/components/"
      via: "import statements"
    - from: "src/lib/components/SettingsModal.svelte"
      to: "src-tauri/src/main.rs"
      via: "invoke('set_api_key')"
---

<objective>
Initialize Tauri 2.0 project with Svelte 5 frontend and verify empty shell builds successfully.

Purpose: Create the foundation that all subsequent phases depend on. A working desktop shell must exist before any feature work.
Output: `math-algorithm-tool/` directory with `npm run tauri dev` working.
</objective>

<context>
@.planning/phases/01-desktop-foundation/01-CONTEXT.md
@.planning/phases/01-desktop-foundation/01-UI-SPEC.md
@.planning/phases/01-desktop-foundation/01-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Phase requirement IDs:** SET-01, SET-02, SET-03, SET-04, SET-05, SET-06

**Project instructions:** Read ./AGENTS.md if exists — follow project-specific guidelines
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Tauri 2.0 project with Svelte 5</name>
  <files>math-algorithm-tool/</files>
  <action>
Initialize the Tauri 2.0 project with Svelte 5 and TypeScript:

1. Run: `npm create tauri-app@latest math-algorithm-tool`
   - Select: Svelte + TypeScript + npm
   - When asked "Initialize with an empty project?", answer "Yes"

2. Navigate into project: `cd math-algorithm-tool`

3. Install dependencies: `npm install`

4. Verify project structure:
   - src/ (Svelte frontend)
   - src-tauri/ (Rust backend)
   - package.json
   - src-tauri/Cargo.toml
   - src-tauri/tauri.conf.json

5. Configure tauri.conf.json:
   - Set app name: "Math Algorithm Tool"
   - Set app identifier: "com.mathalgo.tool"
   - Set window title: "Math Algorithm Tool"
   - Set default window size: 1280x800, min size: 1024x768
   - Enable devtools for debugging

6. Run `npm run tauri dev` to verify empty shell launches successfully

7. Commit: "init: create Tauri 2.0 + Svelte 5 project"
  </action>
  <verify>
    <automated>cd math-algorithm-tool && npm run tauri build -- --debug 2>&1 | head -50</automated>
  </verify>
  <done>
    - `math-algorithm-tool/` directory exists with Tauri 2.0 + Svelte 5 structure
    - `npm run tauri dev` launches a window successfully
    - Project builds without errors
  </done>
  <read_first>
    - src-tauri/tauri.conf.json
    - src-tauri/Cargo.toml
  </read_first>
  <acceptance_criteria>
    - `ls math-algorithm-tool/` shows src/ and src-tauri/ directories
    - `npm run tauri dev` runs without errors
    - Window appears with Tauri title bar
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Phase directory exists with all artifacts
- [ ] Phase goal: Desktop shell working with Tauri 2.0 + Svelte 5
- [ ] Requirements covered: SET-01 through SET-06
</verification>

<success_criteria>
- User can launch the desktop application and see a window
- Application runs without crashes
</success_criteria>

<output>
After completion, create `.planning/phases/01-desktop-foundation/01-01-SUMMARY.md`
</output>