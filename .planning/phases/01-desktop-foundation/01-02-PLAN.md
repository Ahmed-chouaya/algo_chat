---
phase: 01-desktop-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01-PLAN.md
files_modified:
  - math-algorithm-tool/src/app.css
  - math-algorithm-tool/src/lib/components/Header.svelte
  - math-algorithm-tool/src/lib/components/SplitView.svelte
  - math-algorithm-tool/src/lib/components/InputPanel.svelte
  - math-algorithm-tool/src/lib/components/OutputPanel.svelte
  - math-algorithm-tool/src/routes/+page.svelte
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05
  - SET-06
must_haves:
  truths:
    - User can see an input panel for pasting algorithm descriptions
    - User can see an output panel for displaying results
    - UI matches design system from UI-SPEC.md
  artifacts:
    - path: "math-algorithm-tool/src/app.css"
      provides: "Global design system tokens"
    - path: "math-algorithm-tool/src/lib/components/"
      provides: "UI components (Header, SplitView, InputPanel, OutputPanel)"
    - path: "math-algorithm-tool/src/routes/+page.svelte"
      provides: "Main page layout"
  key_links:
    - from: "+page.svelte"
      to: "Header, SplitView"
      via: "import statements"
    - from: "SplitView"
      to: "InputPanel, OutputPanel"
      via: "component slots"
---

<objective>
Build the split-view UI structure matching the UI-SPEC.md design system.

Purpose: Create the input and output panels that enable users to interact with algorithms. This is the primary interface.
Output: Split view layout with header, input panel (40%), and output panel (60%).
</objective>

<context>
@.planning/phases/01-desktop-foundation/01-CONTEXT.md
@.planning/phases/01-desktop-foundation/01-UI-SPEC.md
@.planning/phases/01-desktop-foundation/01-01-SUMMARY.md

**Locked Decisions:**
- D-04: Split View layout (input left, output right)
- D-02: Svelte 5 frontend framework

**Design System:**
- Colors: #0C0A09 (bg), #D4F268 (accent), #1C1917 (surface), #E7E5E4 (foreground)
- Fonts: Newsreader (headings), Instrument Sans (body)
- Effects: Noise overlay, serrated edges, 24px radius on cards
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global styles and design tokens</name>
  <files>math-algorithm-tool/src/app.css</files>
  <action>
Create global CSS with design tokens matching UI-SPEC.md:

1. Create `src/app.css` with CSS custom properties:
   ```css
   :root {
     --bg-base: #0C0A09;
     --color-accent: #D4F268;
     --color-surface: #1C1917;
     --color-foreground: #E7E5E4;
     --color-border: rgba(255, 255, 255, 0.1);
     
     --font-display: 'Newsreader', serif;
     --font-body: 'Instrument Sans', sans-serif;
     --font-mono: system-ui, monospace;
     
     --radius-lg: 24px;
     --radius-full: 9999px;
     
     --transition: 300ms cubic-bezier(0.4, 0, 0.2, 1);
   }
   ```

2. Add global styles:
   - Import Google Fonts via @import or in HTML
   - Body: bg-base color, font-body, 18px base
   - Noise overlay (fixed position SVG, 4% opacity, overlay blend)

3. Add to main.ts or +layout.svelte
  </action>
  <verify>
    <automated>grep -l "color-accent\|Newsreader\|Instrument Sans" math-algorithm-tool/src/app.css</automated>
  </verify>
  <done>
    - Design tokens defined as CSS custom properties
    - Global styles applied to body
    - Fonts loaded from Google Fonts
  </done>
  <read_first>
    - math-algorithm-tool/src/app.html
    - UI-SPEC.md section 1
  </read_first>
  <acceptance_criteria>
    - app.css contains --bg-base: #0C0A09
    - app.css contains --color-accent: #D4F268
    - app.css imports Newsreader and Instrument Sans fonts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build split view layout with header</name>
  <files>math-algorithm-tool/src/lib/components/Header.svelte</files>
  <files>math-algorithm-tool/src/lib/components/SplitView.svelte</files>
  <files>math-algorithm-tool/src/routes/+page.svelte</files>
  <action>
Build the main layout components:

1. Create Header component (56px height, bg-base, bottom border):
   - Logo: 32x32px placeholder
   - App Name: "Math Algorithm Tool" in Newsreader
   - Provider Selector: Dropdown (NVIDIA/OpenAI/Anthropic)
   - Settings Button: 40x40px icon

2. Create SplitView component (40fr 60fr grid):
   - CSS Grid with 40% input, 60% output
   - Min widths to prevent squishing

3. Update +page.svelte with layout structure
  </action>
  <verify>
    <automated>ls math-algorithm-tool/src/lib/components/</automated>
  </verify>
  <done>
    - Header with logo, app name, provider dropdown, settings button
    - Split view with 40%/60% ratio
  </done>
  <read_first>
    - UI-SPEC.md sections 2 and 3
  </read_first>
  <acceptance_criteria>
    - Header has all 4 elements
    - Split view 40/60 ratio
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build input panel with textarea and buttons</name>
  <files>math-algorithm-tool/src/lib/components/InputPanel.svelte</files>
  <action>
Create InputPanel component:

- Background: --color-surface
- Padding: 24px
- Right border: --color-border

- Textarea:
  - Height: 60% (min 200px)
  - Background: --bg-base
  - Border: 1px --color-border
  - Border-radius: 24px
  - Placeholder: "Paste your algorithm description here..."
  - Font: Instrument Sans 18px
  - Resize: vertical

- Import buttons: "Import PDF", "Import Text" (outline style, 9999px radius)

- Submit button:
  - Full width, height 48px
  - Background: --color-accent (#D4F268)
  - Text: --bg-base color, 600 weight, 14px uppercase
  - Border-radius: 9999px
  </action>
  <verify>
    <automated>grep -l "InputPanel" math-algorithm-tool/src/lib/components/</automated>
  </verify>
  <done>
    - Textarea with placeholder
    - Import PDF and Import Text buttons
    - Lime green submit button
  </done>
  <read_first>
    - UI-SPEC.md "Input Panel" section
  </read_first>
  <acceptance_criteria>
    - Textarea placeholder: "Paste your algorithm description..."
    - Submit button: #D4F268 background
    - Buttons: 9999px border-radius
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build output panel with tabs</name>
  <files>math-algorithm-tool/src/lib/components/OutputPanel.svelte</files>
  <action>
Create OutputPanel component:

- Background: --bg-base
- Padding: 24px

- Tab navigation:
  - Three tabs: "Steps", "Code", "Explanation"
  - Font: Instrument Sans 500, 14px
  - Active: --color-foreground, --color-accent underline
  - Inactive: --color-foreground at 60% opacity
  - Use Svelte state for tab switching

- Content area:
  - Background: --color-surface
  - Border-radius: 24px
  - Padding: 24px
  - Placeholder text per tab
  </action>
  <verify>
    <automated>grep -l "OutputPanel" math-algorithm-tool/src/lib/components/</automated>
  </verify>
  <done>
    - Three tabs with switching
    - Active tab has lime underline
    - Content area with placeholder text
  </done>
  <read_first>
    - UI-SPEC.md "Output Panel" section
  </read_first>
  <acceptance_criteria>
    - Three tabs visible
    - Active tab: #D4F268 underline
    - Content area: #1C1917 background, 24px radius
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Design system applied
- [ ] Split view (40/60)
- [ ] Input panel with textarea and buttons
- [ ] Output panel with tabs
</verification>

<success_criteria>
- User can see an input panel for pasting algorithm descriptions
- User can see an output panel for displaying results
</success_criteria>

<output>
After completion, create `.planning/phases/01-desktop-foundation/01-02-SUMMARY.md`
</output>