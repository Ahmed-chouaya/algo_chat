---
phase: 02-algorithm-processing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [INPT-01, INPT-02, INPT-03, INPT-04]
user_setup: []

must_haves:
  truths:
    - "User can paste text containing mathematical algorithm descriptions"
    - "LaTeX math expressions are parsed and extracted"
    - "Plain text mathematical descriptions are handled"
    - "Mixed content (prose + LaTeX) is processed correctly"
  artifacts:
    - path: "src/processing/latex_parser.py"
      provides: "LaTeX expression extraction from text"
      exports: ["extract_latex_expressions", "parse_latex_to_sympy"]
    - path: "src/processing/text_processor.py"
      provides: "Plain text input handling"
      exports: ["process_text_input", "clean_text"]
    - path: "tests/test_latex_parser.py"
      provides: "LaTeX parsing tests"
  key_links:
    - from: "src/processing/latex_parser.py"
      to: "sympy.parsing.latex"
      via: "import parse_latex"
      pattern: "from sympy.parsing.latex import parse_latex"
---

<objective>
Build the input processing layer that handles text, LaTeX, and mixed content input. This is the foundation other plans depend on.
</objective>

<context>
@.planning/phases/02-algorithm-processing/02-CONTEXT.md
@.planning/phases/02-algorithm-processing/02-RESEARCH.md
@.planning/REQUIREMENTS.md

# From CONTEXT.md - Locked Decisions:
- D-01: Full LaTeX support — Parse all standard LaTeX math commands (\sum, \frac, \int, subscripts, matrices)
- D-02: Numbered list presentation (later plan)
- D-03: Color-coded confidence markers (later plan)
- D-04: Stage gates (later plan)

# From RESEARCH.md - Standard Stack:
- SymPy 1.14+ for LaTeX parsing
- latex2sympy2-extended 1.11.0 for comprehensive LaTeX
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LaTeX parser module</name>
  <files>src/processing/latex_parser.py, tests/test_latex_parser.py</files>
  <behavior>
    - Test 1: extract_latex_expressions("$x^2 + y^2$") returns [("x^2 + y^2", SymPyExpr)]
    - Test 2: extract_latex_expressions("The sum $\sum_{i=1}^n i$ equals") returns LaTeX expression
    - Test 3: parse_latex_to_sympy("\\frac{1}{2}") returns Rational(1,2)
    - Test 4: extract_latex_expressions("no math here") returns empty list
    - Test 5: extract_latex_expressions("matrix: $\\begin{pmatrix}1 & 2\\\\ 3 & 4\\end{pmatrix}$") handles matrices
  </behavior>
  <action>
Create src/processing/latex_parser.py with:
1. `extract_latex_expressions(text: str) -> list[tuple[str, Any]]` - Find all LaTeX in $...$ and $$...$$ delimiters, return (latex, parsed) pairs
2. `parse_latex_to_sympy(latex_str: str) -> Expr` - Parse single LaTeX to SymPy, raise on error
3. `is_valid_latex(latex_str: str) -> bool` - Validate without parsing

Handle errors gracefully - return None on parse failure, don't crash.

Create tests/test_latex_parser.py with pytest, verify all behavior cases.
  </action>
  <verify>
    <automated>pytest tests/test_latex_parser.py -x -v</automated>
  </verify>
  <done>
    - LaTeX expressions extracted from text input
    - SymPy expressions available for downstream use
    - Error handling prevents crashes on invalid LaTeX
  </done>
</task>

<task type="auto">
  <name>Task 2: Create text processor module</name>
  <files>src/processing/text_processor.py, tests/test_text_processor.py</files>
  <action>
Create src/processing/text_processor.py with:
1. `process_text_input(raw_text: str) -> ProcessedInput` - Main entry point, returns structured input
2. `clean_text(text: str) -> str` - Strip code blocks, normalize whitespace
3. `split_into_sections(text: str) -> list[Section]` - Split by headers or logical sections
4. `class ProcessedInput` with fields: original_text, cleaned_text, latex_expressions (extracted), sections

Process text in stages:
- Clean: remove code blocks (```...```), normalize
- Extract: call latex_parser.extract_latex_expressions()
- Split: identify logical sections

This feeds both step extraction and plain text handling (INPT-01, INPT-03, INPT-04).
  </action>
  <verify>
    <automated>pytest tests/test_text_processor.py -x</automated>
  </verify>
  <done>
    - Text input cleaned and normalized
    - LaTeX expressions extracted from mixed content
    - Sections identified for step extraction
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire input panel to processor</name>
  <files>src/lib/processing.ts (or equivalent), src/components/AlgorithmInput.svelte</files>
  <read_first>
    - src/components/InputPanel.svelte (from CONTEXT.md - existing component)
  </read_first>
  <action>
1. Create src/lib/processing.ts (or .ts):
   - `processAlgorithmInput(text: string): Promise<ProcessedInput>` - calls text_processor via Tauri command
   
2. Modify src/components/AlgorithmInput.svelte:
   - Import and call processAlgorithmInput on form submit
   - Handle loading state during processing
   - Pass extracted data to OutputPanel Steps tab

3. Create Tauri command in src-tauri/src/processing.rs:
   - `process_input(text: String) -> ProcessedInput` - calls Python text_processor

Connect the flow: InputPanel Submit → Tauri command → Python processor → return to UI
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <done>
    - User can paste algorithm text and submit
    - Text is processed through the pipeline
    - Results available for display
  </done>
</task>

</tasks>

<verification>
- [ ] LaTeX parser extracts expressions from $...$ and $$...$$
- [ ] Text processor handles mixed content (prose + math)
- [ ] Input panel wired to processing pipeline
- [ ] Tests cover core functionality
</verification>

<success_criteria>
User can paste text containing algorithms (INPT-01), LaTeX is parsed (INPT-02), plain text is handled (INPT-03), mixed content extracted (INPT-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-algorithm-processing/02-01-SUMMARY.md`
</output>
